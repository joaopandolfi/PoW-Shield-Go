# TODO and Recommendations

## Outstanding Work Items (from `TODO.md`)

### CSRF Protection
- [x] Implement CSRF token generation and validation
- [x] Use filesystem token to burn session when Redis is disabled
- [x] Use Redis store for temporary session when Redis is enabled
- [x] Add `SameSite=Strict` to the CSRF cookie configuration
- [x] Accept `X-CSRF-Token` header for AJAX requests

### Session Key Prefix
- [x] Use PoW prefix as part of stored session key in cache
  - Current: `session:<sha1_hash>:<nonce_prefix>`
  - Benefit: Allows cache key rotation on prefix change

### Code Quality Fixes
- [x] Fix `gracefullShutdown` typo → `GracefulShutdown()`
- [x] Fix `NewGerator` typo → `NewGenerator()`
- [x] Fix `compelexity` parameter name → `complexity`
- [x] Fix "ised" comment typo → "is used"

---

## Missing Frontend Assets

The following files referenced in `client/public/index.html` are missing from the repository:

| File | Purpose |
|------|---------|
| `client/public/stylesheets/style.css` | Styles for welcome page |
| `client/public/javascripts/main.js` | Client-side script to invoke `powSolver` |
| `client/public/javascripts/config.js` | Client-side configuration (likely server URL, challenge params) |
| `client/public/imgs/logo.png` | Brand logo image |

The compiled bundle `client/public/javascripts/bundle.min.js` is generated by `npm run build`.

---

## Bug Fixes (Priority Order)

### P0 - Resolvidos ✅

1. **Redis `Get()` hardcoded key** (`server/internal/cache/redis.go:53`) ✅ FIXED
   - Fix: `c.client.Get(c.ctx, key)` — variable key used instead of literal "key"

2. **WAF consumes body without restoring** (`server/web/middleware/waf.go:153`) ✅ FIXED
   - Fix: `r.Body = io.NopCloser(bytes.NewReader(body))` restores body for proxy

### P1 - Resolvidos ✅

3. **In-memory cache buffer overflow** (`server/internal/cache/memory.go:45-59`) ✅ FIXED
   - Fix: TTL-based eviction implemented — oldest expired entries removed when buffer > 150

4. **Hardcoded CookieStore secret** (`server/config/config.go:129-143`) ✅ FIXED
   - Fix: `sessionPass()` generates random 32-byte key via `crypto/rand` when SESSION_PASS not set

5. **TLS verification disabled** (`server/internal/request/request.go:16-24`) ✅ FIXED
   - Fix: `InsecureSkipVerify` configurable via `PROTECTED_SERVER_INSECURE_SKIP_VERIFY` (default: `false`)

### P2 - Resolvidos ✅

6. **Redis tests won't compile** (`server/internal/cache/redis_test.go`) ✅ FIXED
   - Fix: Uses correct struct fields (`Server`, not `Port`) and `initializeRedis(ctx, ...)` with context

7. **IP helper malformed** (`server/web/handler/request.go:23-38`) ✅ FIXED
   - Fix: Cleans IP extraction — parses RemoteAddr to remove ports, uses X-Real-Ip header when present

8. **10-minute proxy timeout** (`server/internal/request/request.go:29`) ✅ FIXED
   - Fix: `PROTECTED_SERVER_TIMEOUT_SECONDS` configurable (default: `30s`)

9. **Configuration default mismatch** (`.env.example` vs code defaults) ✅ FIXED
   - Fix: `USE_COOKIE`, `USE_SESSION`, `USE_HEADER` default to `true` — added documentation

10. **Session cookie Secure flag blocks HTTP dev** (`config/config.go:182`) ✅ FIXED
    - Fix: `SESSION_SECURE` defaults based on `USE_TLS` value — auto `false` when TLS disabled


---

## Feature Suggestions

### Short Term
- [x] Add rate limiting per IP (beyond PoW difficulty)
- [x] Add WAF rule logging/dashboard
- [x] Add metrics endpoints (Prometheus format)
- [x] Implement `wafTypes.json` consumer
- [x] Expand `errors.go` with proper error types
- [x] Fix code quality issues (typos, dead code)

### Medium Term
- [x] Add Dockerfile and docker-compose for deployment
- [ ] Add integration tests (Redis, WAF, end-to-end)
- [ ] Add OpenAPI/Swagger spec for API documentation
- [x] Add support for configurable response bodies on blocked requests
- [x] Add WAF rule categories (allow/block only certain categories)

### Long Term
- [ ] Support for multiple backend servers with load balancing
- [ ] WebSocket support
- [ ] Custom rule DSL for dynamic WAF rules
- [x] Admin dashboard for monitoring blocked requests and active sessions
- [x] Admin SPA with configurable path prefix and auth check endpoint
- [x] Admin SPA uses relative paths for compatibility with custom admin path
- [x] Fixed template literal syntax in dashboard.html
- [ ] CAPTCHA fallback for repeated failures instead of difficulty escalation

---

## Code Quality Notes

| Issue | Files | Severity | Status |
|-------|-------|----------|--------|
| Typos in function names | `NewGerator()`, `gracefullShutdown`, `compelexity` | Low | ✅ Fixed |
| Misspelled comment | `injectable.go:13` ("ised") | Low | ✅ Fixed |
| `wafTypes.json` | No longer dead code, used in `waf.go:37-46` | Low | ✅ Fixed |
| `errors.go` | Properly expanded with error codes | Low | ✅ Fixed |
| No structured logging | All files use `log.Println` | Low | ✅ Fixed - now using `log/slog` JSON format |
| No tests for controllers | `pow/`, `proxy/`, `health/` have no tests | Medium | Remaining |
